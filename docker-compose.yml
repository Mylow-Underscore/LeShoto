version: '3.8'

services:
  laboutik:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3103:3103"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3103"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s